import React, { useEffect, useRef } from 'react'
import { FiCommand, FiGlobe, FiBookOpen, FiArrowRight, FiGithub, FiLinkedin, FiMail, FiBriefcase, FiCpu } from 'react-icons/fi'
import Link from 'next/link'
import createGlobe from 'cobe'
import { motion } from 'framer-motion'
import { T, useI18n } from '../components/I18nProvider'
import GitHubProjects from '../components/GitHubProjects'
import GitHubStatsCard from '../components/GitHubStatsCard'
import Visitors from '../components/Visitor'
import { certifications } from '../data/developerProfile'

export const fadeIn = {
  initial: { opacity: 0, y: 8 },
  animate: { opacity: 1, y: 0 },
  transition: { duration: 0.4 }
}

export function MiniGlobe () {
  const canvasRef = useRef()
  const { t } = useI18n()
  useEffect(() => {
    let phi = 0
    let width = 0
    const onResize = () => canvasRef.current && (width = canvasRef.current.offsetWidth)
    window.addEventListener('resize', onResize)
    onResize()
    const globe = createGlobe(canvasRef.current, {
      devicePixelRatio: 1.5,
      width: width * 2,
      height: width * 2,
      phi: 0,
      theta: 0.3,
      dark: 1,
      diffuse: 3,
      mapSamples: 12000,
      mapBrightness: 1.2,
      baseColor: [1, 1, 1],
      markerColor: [251 / 255, 100 / 255, 21 / 255],
      glowColor: [1.2, 1.2, 1.2],
      markers: [{ location: [39.9042, 116.4074], size: 0.05 }],
      onRender: (state) => {
        phi += 0.003
        state.phi = phi
        state.width = width * 2
        state.height = width * 2
      }
    })
    setTimeout(() => { if (canvasRef.current) canvasRef.current.style.opacity = '1' })
    return () => { globe.destroy(); window.removeEventListener('resize', onResize) }
  }, [])
  return (
    <div className="flex flex-col items-center gap-2">
      <div className="w-[200px] h-[200px] md:w-[220px] md:h-[220px] rounded-2xl bg-warm-900 dark:bg-warm-950 overflow-hidden p-2.5">
        <canvas
          ref={canvasRef}
          style={{ width: '100%', height: '100%', opacity: 0, transition: 'opacity 0.8s ease', cursor: 'default' }}
        />
      </div>
      <span className="text-sm text-warm-500 dark:text-warm-400">
        {t('about_location')}
      </span>
    </div>
  )
}

export function VisitorLine () {
  const { count, loading } = Visitors()
  const { t } = useI18n()
  if (loading) return <span className="text-sm text-warm-400">...</span>
  return (
    <span className="text-sm text-warm-500 dark:text-warm-400">
      {count.toLocaleString()} <T k={'visitors_count'} />
    </span>
  )
}

export function LatestHighlights () {
  const { lang } = useI18n()
  const work = {
    company: 'SF',
    role: { zh: '数据产品经理', en: 'Data Product Manager' },
    period: { zh: '2026.02 - 至今', en: 'Feb 2026 - Present' },
    desc: {
      zh: '负责竞品大数据平台（丰景台）功能迭代，推进 BI 智能解读、提示词生成与推荐策略的产品化优化，并通过 UI 结构与信息呈现优化提升核心功能的可理解性与使用效率。',
      en: 'Drove feature iteration and product optimization for a competitive big data platform, enhancing BI insights, recommendations, and overall usability.',
    }
  }
  const research = {
    title: {
      zh: '基于深度学习的地球化学岩石分类',
      en: 'Geochemical rock classification via deep learning'
    },
    institution: { zh: '墨尔本大学', en: 'University of Melbourne' },
    desc: {
      zh: '构建多分支 Transformer，准确率超越传统基线 14%',
      en: 'Multi-branch Transformer achieving 14% accuracy gain over classical baselines'
    }
  }
  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 gap-4 mt-6">
      <Link href="/about#work-experience" className="group p-4 rounded-card border border-warm-100 dark:border-warm-800 hover:border-accent/30 dark:hover:border-accent/30 bg-white dark:bg-warm-950 transition-colors">
        <div className="flex items-center gap-2 mb-2">
          <FiBriefcase className="w-4 h-4 text-accent flex-shrink-0" />
          <span className="text-xs font-medium text-accent uppercase tracking-wider">
            {lang === 'zh' ? '最近工作' : 'Latest Role'}
          </span>
        </div>
        <h4 className="text-sm font-semibold text-warm-900 dark:text-warm-50">
          {work.company} · {work.role[lang]}
        </h4>
        <p className="text-xs text-warm-400 dark:text-warm-500 mt-0.5">{work.period[lang]}</p>
        <p className="text-sm text-warm-600 dark:text-warm-300 mt-2 leading-relaxed">{work.desc[lang]}</p>
      </Link>
      <Link href="/about#research" className="group p-4 rounded-card border border-warm-100 dark:border-warm-800 hover:border-emerald-400/30 dark:hover:border-emerald-400/30 bg-white dark:bg-warm-950 transition-colors">
        <div className="flex items-center gap-2 mb-2">
          <FiCpu className="w-4 h-4 text-emerald-500 flex-shrink-0" />
          <span className="text-xs font-medium text-emerald-500 uppercase tracking-wider">
            {lang === 'zh' ? '最近研究' : 'Latest Research'}
          </span>
        </div>
        <h4 className="text-sm font-semibold text-warm-900 dark:text-warm-50">
          {research.title[lang]}
        </h4>
        <p className="text-xs text-warm-400 dark:text-warm-500 mt-0.5">{research.institution[lang]}</p>
        <p className="text-sm text-warm-600 dark:text-warm-300 mt-2 leading-relaxed">{research.desc[lang]}</p>
      </Link>
    </div>
  )
}

{/* ── Section 1: Hero ── */}

<motion.section {...fadeIn} className="pt-20 pb-16 md:pt-28 md:pb-20">
  <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-8">
    <div className="max-w-xl">
      <h1 className="text-4xl md:text-5xl font-semibold text-warm-900 dark:text-warm-50 tracking-tight">
        <T k={'hero_name'} />
      </h1>
      <p className="mt-3 text-lg text-warm-600 dark:text-warm-300 leading-relaxed">
        <T k={'hero_sub'} />
      </p>
      <div className="mt-6 flex flex-wrap gap-3">
        <Link
          href="/projects"
          className="inline-flex items-center gap-2 px-5 py-2.5 rounded-card bg-accent text-white text-sm font-medium hover:bg-accent-dark transition-colors"
        >
          <T k={'projects'} /> <FiArrowRight className="w-4 h-4" />
        </Link>
        <Link
          href="/blog"
          className="inline-flex items-center gap-2 px-5 py-2.5 rounded-card border border-warm-200 dark:border-warm-700 text-warm-700 dark:text-warm-200 text-sm font-medium hover:bg-warm-50 dark:hover:bg-warm-800 transition-colors"
        >
          <T k={'blog'} /> <FiArrowRight className="w-4 h-4" />
        </Link>
      </div>
    </div>
    <div className="flex-shrink-0">
      <img
        src="https://avatars.githubusercontent.com/u/125345731"
        alt="Nan Sang"
        className="w-28 h-28 md:w-36 md:h-36 rounded-full border-2 border-warm-100 dark:border-warm-800"
      />
    </div>
  </div>
</motion.section>

{/* ── Section 2: About + Globe + Highlights ── */}

<motion.section {...fadeIn} className="py-16 border-t border-warm-100 dark:border-warm-800">
  <div className="flex flex-col md:flex-row md:items-start gap-10">
    <div className="flex-1">
      <h2 className="text-2xl font-semibold text-warm-900 dark:text-warm-50">
        <T k={'about'} />
      </h2>
      <div className="space-y-3 mt-4">
        <div className="flex items-center gap-3 text-warm-700 dark:text-warm-300">
          <FiCommand className="w-5 h-5 text-accent flex-shrink-0" />
          <span><T k={'about_role'} /></span>
        </div>
        <div className="flex items-center gap-3 text-warm-700 dark:text-warm-300">
          <FiGlobe className="w-5 h-5 text-accent flex-shrink-0" />
          <span><T k={'about_school'} /></span>
        </div>
        <div className="flex items-center gap-3 text-warm-700 dark:text-warm-300">
          <FiBookOpen className="w-5 h-5 text-accent flex-shrink-0" />
          <span><T k={'about_major'} /></span>
        </div>
      </div>
      <LatestHighlights />
      <div className="mt-5 flex flex-wrap items-center gap-x-5 gap-y-2">
        {certifications.slice(0, 3).map((cert) => (
          <Link key={cert.name} href="/about#developer-profile"
            className="inline-flex items-center gap-2 text-xs text-warm-500 dark:text-warm-400 hover:text-accent dark:hover:text-accent transition-colors">
            {cert.logo && <img src={cert.logo} alt="" className="w-4 h-4 rounded-sm object-contain" />}
            <span className="truncate max-w-[200px]">{cert.name.split(' Certified')[0]}</span>
          </Link>
        ))}
        <Link href="/about#developer-profile" className="text-xs text-warm-400 dark:text-warm-500 hover:text-accent transition-colors">
          +{certifications.length - 3} more
        </Link>
      </div>
      <div className="mt-4">
        <Link href="/about#developer-profile" className="text-sm text-accent hover:underline inline-flex items-center gap-1">
          <T k={'view_full_profile'} /> <FiArrowRight className="w-3.5 h-3.5" />
        </Link>
      </div>
    </div>
    <div className="flex-shrink-0 hidden md:block">
      <MiniGlobe />
    </div>
  </div>
</motion.section>

{/* ── Section 3: Featured Projects (top 3 + stats) ── */}

<motion.section {...fadeIn} className="py-16 border-t border-warm-100 dark:border-warm-800">
  <div className="flex items-center justify-between mb-8">
    <h2 className="text-2xl font-semibold text-warm-900 dark:text-warm-50">
      <T k={'projects'} />
    </h2>
    <Link href="/projects" className="text-sm text-accent hover:underline inline-flex items-center gap-1">
      <T k={'project_more'} /> <FiArrowRight className="w-3.5 h-3.5" />
    </Link>
  </div>
  <div className="mb-6">
    <GitHubStatsCard />
  </div>
  <GitHubProjects variant="grid" limit={3} />
</motion.section>

{/* ── Section 4: Writing & Content ── */}

<motion.section {...fadeIn} className="py-16 border-t border-warm-100 dark:border-warm-800">
  <div className="flex items-center justify-between mb-8">
    <h2 className="text-2xl font-semibold text-warm-900 dark:text-warm-50">
      <T k={'blog'} />
    </h2>
    <Link href="/blog" className="text-sm text-accent hover:underline inline-flex items-center gap-1">
      <T k={'read_all_posts'} /> <FiArrowRight className="w-3.5 h-3.5" />
    </Link>
  </div>
  <div className="space-y-1">
    <Link href="/note" className="group flex items-center justify-between py-4 border-b border-warm-100 dark:border-warm-800">
      <div>
        <div className="font-medium text-warm-800 dark:text-warm-100 group-hover:text-accent transition-colors">
          <T k={'notes'} />
        </div>
        <p className="text-sm text-warm-500 dark:text-warm-400 mt-0.5"><T k={'notes_desc'} /></p>
      </div>
      <FiArrowRight className="w-4 h-4 text-warm-400 group-hover:text-accent transition-colors flex-shrink-0 ml-4" />
    </Link>
    <Link href="/blog" className="group flex items-center justify-between py-4 border-b border-warm-100 dark:border-warm-800">
      <div>
        <div className="font-medium text-warm-800 dark:text-warm-100 group-hover:text-accent transition-colors">
          <T k={'blog'} />
        </div>
        <p className="text-sm text-warm-500 dark:text-warm-400 mt-0.5"><T k={'blog_desc'} /></p>
      </div>
      <FiArrowRight className="w-4 h-4 text-warm-400 group-hover:text-accent transition-colors flex-shrink-0 ml-4" />
    </Link>
    <Link href="/about" className="group flex items-center justify-between py-4">
      <div>
        <div className="font-medium text-warm-800 dark:text-warm-100 group-hover:text-accent transition-colors">
          <T k={'cv'} />
        </div>
        <p className="text-sm text-warm-500 dark:text-warm-400 mt-0.5"><T k={'cv_desc'} /></p>
      </div>
      <FiArrowRight className="w-4 h-4 text-warm-400 group-hover:text-accent transition-colors flex-shrink-0 ml-4" />
    </Link>
  </div>
</motion.section>

{/* ── Section 5: Contact + Visitors ── */}

<motion.section {...fadeIn} className="py-16 border-t border-warm-100 dark:border-warm-800">
  <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-6">
    <div className="flex items-center gap-5">
      <a href="https://github.com/Neilouo" target="_blank" rel="noreferrer" className="text-warm-500 hover:text-accent transition-colors" aria-label="GitHub">
        <FiGithub className="w-5 h-5" />
      </a>
      <a href="https://www.linkedin.com/in/nan-sang/" target="_blank" rel="noreferrer" className="text-warm-500 hover:text-accent transition-colors" aria-label="LinkedIn">
        <FiLinkedin className="w-5 h-5" />
      </a>
      <a href="mailto:Neilouuo@gmail.com" className="text-warm-500 hover:text-accent transition-colors" aria-label="Email">
        <FiMail className="w-5 h-5" />
      </a>
    </div>
    <VisitorLine />
  </div>
</motion.section>
